# Copy to UI/.env.local

# Where the FastAPI service lives.
VITE_API_BASE=http://localhost:8000
# Never place ADMIN_TOKEN or any other secret in a VITE_* variable: Vite embeds
# it in the public browser bundle. Production admin actions need a trusted
# backend/proxy; local development intentionally permits a blank API token.
