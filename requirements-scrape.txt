# Everything the API needs, plus the browser stage.
#
# Install this wherever a scrape actually runs (GitHub Actions, docker-compose,
# your laptop). The Render API service installs plain requirements.txt so its
# image stays small enough to build on the free tier.
-r requirements.txt

# Naukri's JSON API is reCAPTCHA-gated and its search page is client-rendered,
# so that source has to be rendered in a real browser. Also a LinkedIn fallback.
# After installing, run: playwright install --with-deps chromium
playwright>=1.49
